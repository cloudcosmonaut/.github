name: build template

on: [ push ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - run: |
        echo "Installing dependencies"
        
    - run: |
        echo "Running tests"

    - run: |
        echo "Building"
